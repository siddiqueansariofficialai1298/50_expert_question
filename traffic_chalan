print(" Traffic Challan ")

helmet = input("Helmet (yes/no): ").lower()
license = input("License (yes/no): ").lower()
speed = int(input("Speed: "))

fine = 0

if helmet == "no":
    fine += 1000

if license == "no":
    fine += 2000

if speed > 120:
    fine += 5000

print("Total Fine:", fine)